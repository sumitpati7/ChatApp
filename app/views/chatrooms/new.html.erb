<div class="flex">
    <%= render 'shared/sidebar', users: @users, rooms: @chatrooms %>
    <div class="w-full flex min-h-[calc(100vh_-_70px)] bg-transparent flex-col items-center justify-center">
        <h2 class="text-2xl mb-4 border-b">Create a ChatRoom</h2>
        <%= render 'shared/chatroom_form' %>
    </div>
</div>
